Merge Final eliminado → Unificar Resultados (Code node). El nodo Merge con mergeByPosition fallaba si una tienda devolvía 0 resultados. El nuevo Code node usa $input.all() para recoger los 4 items de los normalizadores, deduplica por URL, y ordena por score+precio. Si una tienda falla por completo, el nodo lo ignora sin error.
Session ID del Agente corregido. Simple Memory ahora usa sessionIdType: "customKey" con sessionKey: "hardware-busqueda-session-v2", una constante fija. Esto elimina el error No session ID found que aparecía cuando el nodo intentaba leer el ID de una expresión dinámica vacía.
AI Agent correctamente configurado. El campo text del agente apunta a ={{ $json.chatInput }}, que viene del nodo Preparar para Agente. Este nodo construye un prompt estructurado con todos los productos y pide MARCA, MODELO, CPU, RAM, GPU, DISCO, SO. El nodo Parsear Respuesta Agente tiene fallback robusto si el modelo devuelve JSON malformado.
Bucle de calidad. Contar Productos → Validar Calidad (IF ≥ 3) → si falla, Reformular Busqueda amplía las keywords y relanza las 4 búsquedas en paralelo.
PDF corporativo con logo. El logo logo_innovatek_black_web.png está embebido como data:image/png;base64,... directamente en el HTML, por lo que Gotenberg no necesita acceder al sistema de archivos para renderizarlo. Formato A4 landscape (11.69" × 8.27"), con Top 3 global + tabla por tienda, botones "Ver Oferta" azules, tipografía Inter/Segoe UI.